base: http://localhost:9000

plan:
  - name: Fetch
    request:
      url: /
    tags:
      - requests
    assign: val
  - name: assert response
    assert:
      key: val.status
      value: 200
    tags: ["asserts"]