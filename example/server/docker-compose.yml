version: "3.9"
services:
  floodr-server:
    ports:
      - "9000:9000"
    image: floodr-example-server:latest
    build:
      context: ./
      dockerfile: Dockerfile-example-server
      #args:
      #  buildno: 1
    environment:
        OUTPUT: 0
        DELAY_MS: 100
