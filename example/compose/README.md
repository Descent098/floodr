# Floodr Docker Demo

This setup includes floodr as a docker image. Modify `benchmark.yml` with your parameters, and integrate your web server. There is a pre-included python flask server for testing. 

To run use:

```bash
docker compose up
```
